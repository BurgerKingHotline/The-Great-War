###########################
# Nuclear Bomb Events
###########################

add_namespace = TLG_newsevents

# senussiya revolts!
news_event = {
	id = TLG_newsevents.1
	title = TLG_newsevents.1.t
	desc = TLG_newsevents.1.desc
	picture = GFX_news_event_nuke

	major = yes

	is_triggered_only = yes

	fire_only_once = yes
	
	option = { #eh we can defeat them
		name = decisions.1.a
		trigger = {
			OR = {
				original_tag = FROM
				is_in_faction_with = FROM
				is_subject_of = FROM
				FROM = {
					is_subject_of = ROOT
				}
			}
		}
	}

	option = { #Great!
		name = decisions.1.b
		trigger = {
			has_war_with = FROM
		}
	}

	option = { #hopefully this wont inspire our colonies...
		name = decisions.1.c
		trigger = {
			NOT = {
				original_tag = FROM
				is_in_faction_with = FROM
				is_subject_of = FROM
				FROM = {
					is_subject_of = ROOT
				}
				has_war_with = FROM
			}
			any_owned_state = {
				is_in_home_area = NO
			}
		}
	}

	option = { #good for them ig
		name = decisions.1.c
		trigger = {
			NOT = {
				original_tag = FROM
				is_in_faction_with = FROM
				is_subject_of = FROM
				FROM = {
					is_subject_of = ROOT
				}
				has_war_with = FROM
				any_owned_state = {
					is_in_home_area = NO
				}
			}
		}
	}
}

#The Russian Revolution
news_event = {
	id = TLG_newsevents.2
	title = TLG_newsevents.2.t
	desc = TLG_newsevents.2.desc
	picture = GFX_news_event_nuke

	major = yes

	is_triggered_only = yes

	fire_only_once = yes
	
	option = { #We can defeat these traitors to the tsar!
		name = decisions.2.a
		trigger = {
			OR = {
				original_tag = FROM
				is_in_faction_with = FROM
				is_subject_of = FROM
				FROM = {
					is_subject_of = ROOT
				}
			}
			NOT = {
				has_government = communism
			}
		}
	}

	option = { #Great!
		name = decisions.2.b
		trigger = {
			has_war_with = FROM
		}
	}

	option = { #These commies will collapse anyways
		name = decisions.2.c
		trigger = {
			NOT = {
				original_tag = FROM
				is_in_faction_with = FROM
				is_subject_of = FROM
				FROM = {
					is_subject_of = ROOT
				}
				has_war_with = FROM
				any_owned_state = {
					is_in_home_area = NO
				}
				has_government = communism
			}
		}
	}

	option = { #Another state falls to the Revolution!
		name = decisions.2.d
		trigger = {
			has_government = communism
		}
	}
}

news_event = { #Russia proclaims a crusade against communism
	id = TLG_newsevents.3
	title = TLG_newsevents.3.t
	desc = TLG_newsevents.3.desc
	picture = GFX_news_event_nuke

	major = yes

	is_triggered_only = yes

	fire_only_once = yes
	
	option = { #For the Tsar and the destruction of Communism!
		name = decisions.3.a
		trigger = {
			OR = {
				original_tag = FROM
				is_in_faction_with = FROM
				is_subject_of = FROM
				FROM = {
					is_subject_of = ROOT
				}
			}
		}
	}

	option = { #Great!
		name = decisions.3.b
		trigger = {
			NOT = {
				has_government = communism
				has_war_with = FROM
			}
		}
		if = {
			limit = {
				OR = {
					has_government = fascism
					any_enemy_country = {
						has_government = communism
					}
				}
			}
		}
		hidden_effect = {
			add_opinion_modifier = {
				target = FROM
				modifier = crusade_against_communism_positive_modifier
			}
		}
	}

	option = { #You cannot destroy the revolution!
		name = decisions.3.c
		trigger = {
			has_government = communism
		}
		hidden_effect = {
			add_opinion_modifier = {
				target = FROM
				modifier = crusade_against_communism_negative_modifier
			}
		}
	}
}